{% extends 'base.html.twig' %}

{% block title %}Hospital Admin Page{% endblock %}

{% block body %}
    <div class="col-12">
        {{ form_start(form, {attr: {novalidate: 'novalidate'}}) }}
        {{ form_widget(form) }}
        {{ form_end(form) }}
    </div>
{% endblock %}
